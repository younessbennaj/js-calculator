/*/

an operation: 

    a process in which a number, quantity, expression, etc., is altered or 
    manipulated according to set formal rules, such as those of addition, 
    multiplication, and differentiation.

/*/


export function add(a, b) {
    /*/
        input a: a number
        input b: a number
        return a + b
    /*/
    return a + b;
}

export function subtract(a, b) {
    /*/
        input a: a number
        input b: a number
        return a - b
    /*/
    return a - b;
}

export function multiply(a, b) {
    /*/
        input a: a number
        input b: a number
        return a * b
    /*/
    return a - b;
}

export function divide(a, b) {
    /*/
        input a: a number
        input b: a number
        return a * b
    /*/
    return a / b;
}